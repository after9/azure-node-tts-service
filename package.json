{
  "private": true,
  "name": "azure-node-tts-service",
  "version": "1.0.0",
  "description": "A template which base on Azure TTS service with Node.js",
  "main": "server.js",
  "scripts": {
    "dev": "export NODE_ENV='dev' && nodemon server.js",
    "prod": "export NODE_ENV='production' && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "After9",
  "license": "MIT",
  "dependencies": {
    "@koa/cors": "^5.0.0",
    "https-proxy-agent": "^3.0.0",
    "koa": "^2.15.2",
    "koa-body": "^6.0.1",
    "koa-parameter": "^3.0.1",
    "koa-router": "^12.0.1",
    "koa-static": "^5.0.0",
    "microsoft-cognitiveservices-speech-sdk": "^1.34.0",
    "readline": "^1.3.0"
  }
}
